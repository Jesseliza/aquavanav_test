# ✅ Server
PORT=3000
NODE_ENV=development

# ✅ PostgreSQL (Used by Drizzle ORM and backend)
DATABASE_URL=postgresql://postgres:Postgres%40123@localhost:5432/aquanav

# ✅ Auth (if needed)
JWT_SECRET=your_jwt_secret_key
JWT_EXPIRES_IN=1d

# ✅ Client (Vite - available via import.meta.env.VITE_API_BASE_URL)
VITE_API_BASE_URL=http://localhost:3000

# ✅ Replit-specific (only if using Replit)
REPL_ID=your-replit-id
SESSION_SECRET="your-strong-session-secret"
